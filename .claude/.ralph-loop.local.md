# Ralph Loop: Boiler Template Standardization — Phase 1

## Mission

Establish `obsidian-boiler-template` as the complete source of truth for all Obsidian plugin shared patterns. Create all source files, expand the sync engine, add enforcement layers, and self-apply.

## Plan Reference

Read the full plan: `/Users/beomsu/Documents/Dev/Obsidian-Plugins/.claude/plans/boiler-template-standardization.md`

## Working Directory

`/Users/beomsu/Documents/Dev/Obsidian-Plugins/obsidian-boiler-template/`

## Waves to Execute (in order)

### Wave 1: Create Source Files in `tooling/shared/` (14 files)

#### Infra artifacts (5 files):
1. `tooling/shared/.editorconfig` — tab/4, `[*.yml]` space/2 override (YAML spec requires it), `[*.md]` trim_trailing_whitespace=false
2. `tooling/shared/eslint.base.js` — Export as named array: `export const baseConfig = [...]`. Include tseslint base + shared rules. CRITICAL: `no-console` rule must be scoped to `{ files: ['src/**/*.ts'] }` ONLY. Do NOT ban console in `scripts/`, `tooling/`, or `*.mjs` files. `plugin-logger.ts` uses `// eslint-disable-next-line no-console` per call site.
3. `tooling/shared/commitlint.config.mjs` — `extends: ['@commitlint/config-conventional']`
4. `tooling/shared/.husky/pre-commit` — `npx lint-staged` (file must be chmod +x)
5. `tooling/shared/.husky/commit-msg` — `npx --no -- commitlint --edit $1` (file must be chmod +x)

#### GitHub templates (4 files):
6. `tooling/shared/.github/ISSUE_TEMPLATE/bug_report.yml` — YAML form with required fields: description, reproduction steps, plugin version, Obsidian version, OS multi-select dropdown. Auto-labels: `["bug", "needs-triage"]`
7. `tooling/shared/.github/ISSUE_TEMPLATE/feature_request.yml` — YAML form: what (required) + why (required). Keep it minimal (2 fields). Auto-labels: `["feature", "needs-triage"]`
8. `tooling/shared/.github/ISSUE_TEMPLATE/config.yml` — `blank_issues_enabled: false` with contact link to author profile
9. `tooling/shared/.github/PULL_REQUEST_TEMPLATE.md` — Summary, Changes, Testing checklist (`pnpm run ci` passes, tested in Obsidian), Checklist (conventional commits, no debug code)

#### Shared code modules (5 files):
10. `tooling/shared/src-shared/plugin-notices.ts` — Catalog-driven notice system. Generic `PluginNoticesHost` interface (not coupled to any specific plugin type). `{{param}}` interpolation, mute/unmute persisted in `settings.plugin_notices.muted`, dedup by ID (reuse existing Notice instance), prefix constructor arg (e.g. `'[QMD]'`). Reference: Smart Connections `/Users/beomsu/Documents/Dev/Obsidian-Plugins/obsidian-smart-connections/src/app/notices.ts`
11. `tooling/shared/src-shared/plugin-logger.ts` — Structured prefixed logger. Format: `[PREFIX] level | message | key=value`. `isDebug: () => boolean` constructor arg for live settings read. Log levels: debug (silent unless isDebug), info, warn, error. `noticeError(msg, error?)` bridges to `new Notice()`. NO dependency on PluginNotices. Uses `// eslint-disable-next-line no-console` per console call.
12. `tooling/shared/src-shared/debounce-controller.ts` — Self-contained debounce with rerun semantics. Zero Obsidian imports. If operation running when `markDirty()` called, sets `rerun=true` for automatic second run. `dispose()` cancels timer. Reference: QMD `/Users/beomsu/Documents/Dev/Obsidian-Plugins/obsidian-qmd/src/qmd/auto-sync.ts`
13. `tooling/shared/src-shared/settings-migration.ts` — Generic migration helper. Each migration function receives the FULL data object `(data: Record<string, unknown>) => Record<string, unknown>` to support conditional transforms. Returns mutated copy. Only saves if at least one migration changed data. Reference pattern: Bible Search `/Users/beomsu/Documents/Dev/Obsidian-Plugins/obsidian-bible-search/src/main.ts` lines 35-57
14. `tooling/shared/src-shared/styles.base.css` — CSS variable fallback skeleton with 3-layer pattern: `var(--semantic-token, var(--color-token, #fallback))`. Plugin prefix convention documented in comments. Reference: Smart Connections `/Users/beomsu/Documents/Dev/Obsidian-Plugins/obsidian-smart-connections/src/styles.css`

### Wave 2: Sync Engine Expansion

Edit `tooling/sync/index.mjs`:

Item 1: Add 14 new `SHARED_FILE_SPECS` entries with these exact source-destination mappings:

| Source path segments | Destination path segments | chmod |
|---------------------|--------------------------|-------|
| `['tooling','shared','.editorconfig']` | `['.editorconfig']` | no |
| `['tooling','shared','eslint.base.js']` | `['eslint.base.js']` | no |
| `['tooling','shared','commitlint.config.mjs']` | `['commitlint.config.mjs']` | no |
| `['tooling','shared','.husky','pre-commit']` | `['.husky','pre-commit']` | yes |
| `['tooling','shared','.husky','commit-msg']` | `['.husky','commit-msg']` | yes |
| `['tooling','shared','.github','ISSUE_TEMPLATE','bug_report.yml']` | `['.github','ISSUE_TEMPLATE','bug_report.yml']` | no |
| `['tooling','shared','.github','ISSUE_TEMPLATE','feature_request.yml']` | `['.github','ISSUE_TEMPLATE','feature_request.yml']` | no |
| `['tooling','shared','.github','ISSUE_TEMPLATE','config.yml']` | `['.github','ISSUE_TEMPLATE','config.yml']` | no |
| `['tooling','shared','.github','PULL_REQUEST_TEMPLATE.md']` | `['.github','PULL_REQUEST_TEMPLATE.md']` | no |
| `['tooling','shared','src-shared','plugin-notices.ts']` | `['src','shared','plugin-notices.ts']` | no |
| `['tooling','shared','src-shared','plugin-logger.ts']` | `['src','shared','plugin-logger.ts']` | no |
| `['tooling','shared','src-shared','debounce-controller.ts']` | `['src','shared','debounce-controller.ts']` | no |
| `['tooling','shared','src-shared','settings-migration.ts']` | `['src','shared','settings-migration.ts']` | no |
| `['tooling','shared','src-shared','styles.base.css']` | `['src','shared','styles.base.css']` | no |

Item 2: Add `chmod` flag support. Add optional `chmod` boolean property to spec shape. In `applySyncPlan()`, after `fs.writeFileSync()`, if `operation.chmod === true`, call `fs.chmodSync(filePath, 0o755)`.

Item 3: Add `sync.skipDestinations` support. In `buildSyncPlan()`, before creating each write operation, check if the destination path array matches any entry in `config.sync?.skipDestinations` (deep array equality). If matched, skip that file.

Item 4: Add `'prepare'` to `REQUIRED_SCRIPT_NAMES`.

Item 5: Add `prepare: 'husky'` to `REQUIRED_SCRIPT_VALUES`.

Item 6: Add devDependency validation in `validateManagedRepoContract()`. After the `.npmrc` check block, read `package.json` and assert `devDependencies.husky` exists and `devDependencies['lint-staged']` exists. Use the same `assert()` pattern as existing checks.

Item 7: Add `config.sync?.skipDestinations` validation in `validateBoilerConfig()`. If present, validate it is `string[][]` (array of string arrays where each inner array contains only strings).

Item 8: Add `config.notices?.prefix` validation in `validateBoilerConfig()`. If present, validate it is a non-empty string.

### Wave 3: CI/Release Enforcement

Item 1: Add `sync:check` step to `renderCiWorkflow()`. Insert AFTER `pnpm install --frozen-lockfile` and BEFORE `pnpm run ci`:
```yaml
- name: Check boiler-template sync
  run: pnpm sync:check
```

Item 2: Edit `tooling/shared/release.mjs`. Add a `sync:check` call BEFORE the existing `pnpm run ci` call. Use the same subprocess execution pattern already in the file (look at how `pnpm run ci` is called and replicate for `pnpm sync:check`).

### Wave 4: Self-Apply + Verify

Item 1: Add to `package.json` scripts: `"prepare": "husky"`

Item 2: Add devDependencies: `husky`, `lint-staged`, `@commitlint/cli`, `@commitlint/config-conventional`

Item 3: Add to `package.json`: `"lint-staged": { "src/**/*.ts": ["eslint --fix"], "scripts/**/*.{js,mjs}": ["eslint --fix"] }`

Item 4: Update `eslint.config.js` to import `baseConfig` from `./eslint.base.js` and spread it. Remove duplicated rules that are now in the base. Keep any boiler-template-specific overrides.

Item 5: Run `pnpm install` — this updates `pnpm-lock.yaml`

Item 6: Run `pnpm prepare` (initialize husky hooks)

Item 7: CRITICAL — Stage and commit BOTH `package.json` AND `pnpm-lock.yaml` changes. CI uses `--frozen-lockfile` and will fail if lockfile is stale.

Item 8: Run `pnpm run ci` — MUST PASS (build + lint + test)

Item 9: Run `pnpm sync:plugins --dry-run` — verify output shows expected state for all 6 targets:
- `obsidian-boiler-template` (self) shows UNCHANGED (already self-applied)
- `obsidian-qmd` shows pending changes (14 new files)
- `obsidian-eagle-plugin` shows pending changes
- `Metadata-Auto-Classifier` shows pending changes
- `obsidian-bible-search` shows pending changes
- `obsidian-smart-connections` shows pending changes

## Verification Criteria

After each wave, verify:
- `pnpm run ci` passes (build + lint + test)
- No TypeScript errors
- No ESLint errors

After all waves:
- All 14 source files exist in `tooling/shared/`
- `pnpm sync:plugins --dry-run` shows expected state for all 6 targets
- `pnpm sync:check` passes (boiler-template itself is in sync)
- Husky hooks are executable: `ls -la .husky/pre-commit .husky/commit-msg` shows `-rwxr-xr-x`
- `pnpm-lock.yaml` is committed and not stale

## Constraints

- Default branch is `master` (not main)
- Tab indentation, width 4 (enforced by .editorconfig)
- NO `git tag`, `git push --tags`, `gh release` — DENIED by settings.json
- PluginNotices and PluginLogger must have ZERO coupling to specific plugin types
- `eslint.base.js` must export a NAMED ARRAY (`export const baseConfig = [...]`) that plugins can spread
- `no-console` ESLint rule MUST be scoped to `{ files: ['src/**/*.ts'] }` ONLY
- `plugin-logger.ts` uses `// eslint-disable-next-line no-console` per console call site
- `debounce-controller.ts`, `settings-migration.ts`, `plugin-logger.ts` must have ZERO Obsidian imports
- `plugin-notices.ts` needs Obsidian `Notice` API — that is OK

## Completion

When ALL waves complete and ALL verifications pass, output:

<promise>BOILER COMPLETE</promise>
